#include <iostream>
int max_length(std::string& str){
    std::string
    for(int i = 0 ; i < str.size, ++i){
        for(int j = 0; j < str.size; ++j){
            if(str[i] == " "){=
                str[i] = "/0";

            }
            ++i;
        }
    }
}